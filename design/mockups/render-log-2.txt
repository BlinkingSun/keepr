Generating the teal main window and teal icon first, then editing only the accent hue for the blue pair so they stay like-for-like.Fixing the broken grid row, then creating blue accent variants via image_edit.Cleaning the teal grid row sequence, then deriving the blue grid from that fixed frame.Fixing remaining row-number duplicates, then producing the blue pair from the clean teal frame.Image model keeps mangling row numbers. Building the grid mockups in HTML for structural accuracy, then screenshotting.- `design/mockups/05-accent-teal.png` — Approved three-pane grid with teal selection/nav/primary; 12 sequential filled rows; amber missing-vendor on row 5; dark inspector fields; live totals, no sync.
- `design/mockups/06-accent-blue.png` — Identical layout/content to 05 with blue accent only, for direct side-by-side comparison.
- `design/mockups/07-icon-teal.png` — K + receipt-edge mark at 256px and 32px, teal accent, no wordmark.
- `design/mockups/08-icon-blue.png` — Same K-mark geometry at both sizes with blue accent only.
